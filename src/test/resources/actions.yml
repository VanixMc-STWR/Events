# Reusable actions
actions:
  reward-base:
    type: message
    format: chat
    audience: global
    message: "Hello World!"

  rare-reward:
    id: reward-base
    message: "Hello, Rare World!!"

  epic-reward:
    id: rare-reward
    message: "Hello, Epic World!!!"  # Overrides currency, inherits from rare-reward which inherited from reward-base

  command-test:
    type: command
    sender: player
    command: "gmc"